# 参与开发

1. 安装 Node.js 22+ 和 MoonBit 工具链。
2. 运行 `npm run check` 和 `npm test`。
3. 使用 `npm run demo` 创建可丢弃的合成样例，不用个人文件做开发测试。
4. 核心规则放在 MoonBit，文件系统行为放在 `host`。
5. 修改执行或恢复逻辑时，添加对应的真实文件异常测试。

提交问题时，请附上工具链版本、操作系统、规则 JSON、预期和实际行为。不要上传私人文件、敏感路径或 `.filenest` 日志；优先提供合成的最小复现样例。

代码规模使用 `node scripts/count-lines.mjs` 统计。请勿通过拆分空行、复制测试或加入生成代码增加行数。

